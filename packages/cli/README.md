# @solard/cli

CLI adapter for `@solard/sdk`.

```bash
slrd wallet create trader-1
slrd wallets
slrd token <mint> my-token
slrd buy my-token --wallet trader-1 --sol 0.05
slrd sell my-token --wallet trader-1
```

Set `SLRD_MASTER_KEY` for persisted wallets and `RPC_ENDPOINT` for chain operations.
