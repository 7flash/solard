{
  "name": "solard-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "cli": "bun run packages/cli/bin/solard.ts",

    "pub:bump": "bun run version:core && bun run version:cli && bun run version:solard-cli",
    "version:core": "cd packages/core && bun pm version patch --no-git-tag-version",
    "version:cli": "cd packages/cli && bun pm version patch --no-git-tag-version",
    "version:solard-cli": "cd packages/solard-cli && bun pm version patch --no-git-tag-version",

    "pub:lock": "bun install --lockfile-only",

    "pub:core": "cd packages/core && bun publish --access public --no-git-checks",
    "pub:cli": "cd packages/cli && bun publish --access public --no-git-checks",
    "pub:solard-cli": "cd packages/solard-cli && bun publish --access public --no-git-checks",

    "pub": "bun run pub:bump && bun run pub:lock && bun run pub:core && bun run pub:cli && bun run pub:solard-cli"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3"
  }
}
